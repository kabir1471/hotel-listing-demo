import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import HotelDetailScreen from './HotelDetailView';

const HotelDetail = () => {
  return <HotelDetailScreen />;
};

export default HotelDetail;

const styles = StyleSheet.create({});
